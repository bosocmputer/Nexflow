services:
  postgres:
    image: postgres:16-alpine
    container_name: nexflow-shopee-gateway-postgres
    environment:
      POSTGRES_USER: nexflow_gateway
      POSTGRES_PASSWORD: ${SHOPEE_GATEWAY_DB_PASSWORD:?required}
      POSTGRES_DB: nexflow_shopee_gateway
    volumes:
      - gateway_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nexflow_gateway -d nexflow_shopee_gateway"]
      interval: 5s
      timeout: 5s
      retries: 20
    restart: unless-stopped

  gateway:
    build:
      context: ${NEXFLOW_RELEASE_DIR:-/mnt/data/nextstep-node-2/nexflow-release}/backend
      dockerfile: Dockerfile.gateway
    container_name: nexflow-shopee-gateway
    env_file:
      - .env
    environment:
      PORT: "8091"
      DATABASE_URL: postgresql://nexflow_gateway:${SHOPEE_GATEWAY_DB_PASSWORD}@postgres:5432/nexflow_shopee_gateway?sslmode=disable
      SHOPEE_GATEWAY_TENANT_REGISTRY: /app/config/nextstep-instances.json
    volumes:
      - ${NEXFLOW_RELEASE_DIR:-/mnt/data/nextstep-node-2/nexflow-release}/deploy/nextstep-instances.json:/app/config/nextstep-instances.json:ro
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      default:
        aliases:
          - shopee-gateway
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped

volumes:
  gateway_postgres_data:
